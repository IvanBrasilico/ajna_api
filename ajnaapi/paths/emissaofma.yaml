post:
  summary: Recebe Evento de emissão de FMA pelo Recinto
  tags:
    - API Recintos Xtra
  requestBody:
    description: Conteúdo do Evento
    content:
      application/json:
        schema:
          $ref: '../schemas/models_extra.yaml/EmissaoFMA'
  responses:
    201:
      description: Resposta em caso de sucesso
      content:
        application/json:
          schema: {}
    500:
      description: Erro
      content:
        application/json:
          schema: {}
  security:
    - jwt: ['secret']
